<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Vista de búsqueda -->
    <record id="estate_property_search_view" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Buscar propiedades">

                <!-- Campos usados en la búsqueda rápida -->
                <field name="name"/>
                <field name="post_code"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>

                <!-- Filtros -->
                <filter name="available_filter"
                        string="Available"
                        domain="[('state', '=', 'new')]"/>

                <!-- Agrupación -->
                <group expand="0" string="Group By">
                    <filter name="group_post_code"
                            string="Postcode"
                            context="{'group_by': 'post_code'}"/>
                </group>

            </search>
        </field>
    </record>



    <!-- Acción para abrir la vista de estate.property -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Propiedades</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="estate_property_search_view"/>
        <field name="context">
            {'search_default_available_filter': 1}
        </field>
    </record>

    <!-- Vista árbol (lista) -->
    <record id="estate_property_list_view" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list 
            decoration-success="state == 'offer_received' or state == 'offer_accepted'"
            decoration-bf="state == 'offer_accepted'"
            decoration-muted="state == 'sold'"
            >

                <field name="name"/>
                <field name="post_code"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availabality" optional="hide"/>
                <field name="state" optional="hide"/>
            </list>
        </field>
    </record>

    


 
    <!-- Vista formulario -->
    <record id="estate_property_form_view" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Propiedad">

                <!-- Barra de estado -->
                <header>
                    <field name="state" widget="statusbar"
                        statusbar_visible="new,offer_received,offer_accepted,sold,canceled"/>

                    <button name="action_cancel"
                            type="object"
                            string="Cancelar"
                            class="btn btn-secondary"
                            invisible="state in ('sold','canceled')"/>

                    <button name="action_sold"
                            type="object"
                            string="Vendido"
                            class="btn btn-primary"
                            invisible="state in ('sold','canceled')"/>

                </header>

                <sheet>

                    <!-- Título grande de la propiedad -->
                    <h1>
                        <field name="name"/>
                    </h1>

                    <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>

                    <!-- Bloque superior con dos columnas -->
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': True, 'no_open': True}"/>
                            <field name="post_code"/>
                            <field name="date_availabality"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>

                    <!-- Pestañas -->
                    <notebook>

                        <page string="Descripción">
                            <group>
                                <field name="description"/>
                            </group>
                        </page>

                        <page string="Detalles">
                            <group>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Estados">
                            <group>
                                <field name="active"/>
                                <field name="state"/>
                            </group>
                        </page>
                        <page string="Otra información">
                            <group>
                                <field name="seller_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                        <page string="Offers">
                        
                            <field name="offer_ids" 
                            readonly="state in ('offer_accepted','sold','canceled')">
                                <list editable="bottom"
                                decoration-danger="status == 'refused'"
                                decoration-success="status == 'accepted'"
                                >
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <button name="action_accept"
                                            states="draft"
                                            type="object"
                                            icon="fa-check"
                                            invisible="status"/>
                                    <button name="action_refuse"
                                            states="draft"
                                            type="object"
                                            icon="fa-times"
                                            invisible="status"/>
                                    <field name="status" column_invisible="1"/>
                                </list>
                            </field>
                        </page>


                    </notebook>

                </sheet>
            </form>
        </field>
    </record>

    <!-- Vista formulario para estate.property.offer
    <record id="estate_property_offer_form_view" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Offer">
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                        <field name="status"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    -->


</odoo>
